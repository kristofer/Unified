// Test: Compound assignments without semicolons
fn compound_no_semi() -> Int {
    let mut total = 100
    total += 50
    total -= 30
    total *= 2
    return total
}

fn main() -> Int {
    return compound_no_semi()
}
