// Test 19: Generic function with arithmetic operations
// Tests that generic code can use operators

fn add_one<T>(x: T) -> T {
    // In a real implementation, this would use operator constraints
    // For now, just return the value
    return x
}

fn main() -> Int {
    let result = add_one(9)
    
    // Should return the value as-is
    if result == 9 {
        return 1
    }
    return 0
}
