// Test 16: Nested generic function calls
// Tests calling generic functions from within other generic functions

fn inner<T>(x: T) -> T {
    return x
}

fn outer<U>(y: U) -> U {
    let temp = inner(y)
    return temp
}

fn main() -> Int {
    let result = outer(100)
    
    if result == 100 {
        return 1
    }
    return 0
}
